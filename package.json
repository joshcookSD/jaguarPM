{
  "name": "jaguarpm",
  "version": "1.0.0",
  "description": "project excellence management",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/joshcookSD/jaguarPM.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/joshcookSD/jaguarPM/issues"
  },
  "homepage": "https://github.com/joshcookSD/jaguarPM#readme",
  "dependencies": {
    "concurrently": "^3.5.1",
    "rimraf": "^2.6.2"
  },
  "scripts": {
    "build": "concurrently \"cd jaguar-client && yarn build\" \"cd jaguar-server && yarn build\"",
    "clean": "concurrently \"rimraf node_modules yarn.lock\" \"cd jaguar-client && rimraf node_modules build yarn.lock\" \"cd jaguar-server && rimraf node_modules build yarn.lock\"",
    "heroku-postbuild": "yarn build",
    "install": "(cd jaguar-client && yarn) && (cd jaguar-server && yarn)",
    "start": "concurrently \"cd jaguar-client && yarn start\" \"cd jaguar-server && yarn start\"",
    "start:prod": "cd jaguar-server && yarn start:prod"
  }
}
